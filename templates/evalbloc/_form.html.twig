{{ form_start(form) }}
    <div class="text-danger">
        {{ form_errors(form.name) }}
    </div>
<div>
    {{ form_widget(form.name) }}
</div>
    <button class="button button-primary mb-4">{{ button_label|default('Créer') }}</button>
{{ form_end(form) }}
