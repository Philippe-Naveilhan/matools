{% extends 'base.html.twig' %}

{% block title %}Evalbloc{% endblock %}

{% block body %}
    <h1>Evalbloc</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ evalbloc.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ evalbloc.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('evalbloc_index') }}">back to list</a>

    <a href="{{ path('evalbloc_edit', {'id': evalbloc.id}) }}">edit</a>

    {{ include('evalbloc/_delete_form.html.twig') }}
{% endblock %}
