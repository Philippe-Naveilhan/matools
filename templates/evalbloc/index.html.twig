{% extends 'base.html.twig' %}

{% block title %}Evalbloc index{% endblock %}

{% block body %}
    <h1>Evalbloc index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for evalbloc in evalblocs %}
            <tr>
                <td>{{ evalbloc.id }}</td>
                <td>{{ evalbloc.name }}</td>
                <td>
                    <a href="{{ path('evalbloc_show', {'id': evalbloc.id}) }}">show</a>
                    <a href="{{ path('evalbloc_edit', {'id': evalbloc.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('evalbloc_new') }}">Create new</a>
{% endblock %}
