{% extends 'base.html.twig' %}

{% block title %}Circo{% endblock %}

{% block body %}
    <h1>Circo</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ circo.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ circo.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('circo_index') }}">back to list</a>

    <a href="{{ path('circo_edit', {'id': circo.id}) }}">edit</a>

    {{ include('circo/_delete_form.html.twig') }}
{% endblock %}
