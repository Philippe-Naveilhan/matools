{% extends 'base.html.twig' %}

{% block title %}Evalstudent{% endblock %}

{% block body %}
    <h1>Evalstudent</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ evalstudent.id }}</td>
            </tr>
            <tr>
                <th>Comment</th>
                <td>{{ evalstudent.comment }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('evalstudent_index') }}">back to list</a>

    <a href="{{ path('evalstudent_edit', {'id': evalstudent.id}) }}">edit</a>

    {{ include('evalstudent/_delete_form.html.twig') }}
{% endblock %}
