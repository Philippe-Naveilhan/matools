{% extends 'base.html.twig' %}

{% block title %}Evalcategory{% endblock %}

{% block body %}
    <h1>Evalcategory</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ evalcategory.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ evalcategory.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('evalcategory_index') }}">back to list</a>

    <a href="{{ path('evalcategory_edit', {'id': evalcategory.id}) }}">edit</a>

    {{ include('evalcategory/_delete_form.html.twig') }}
{% endblock %}
