{% extends 'base.html.twig' %}

{% block title %}Evalcompetence{% endblock %}

{% block body %}
    <h1>Evalcompetence</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ evalcompetence.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ evalcompetence.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('evalcompetence_index') }}">back to list</a>

    <a href="{{ path('evalcompetence_edit', {'id': evalcompetence.id}) }}">edit</a>

    {{ include('evalcompetence/_delete_form.html.twig') }}
{% endblock %}
