{% extends 'base.html.twig' %}

{% block title %}Etiquettes Casier !{% endblock %}

{% block body %}

<div>
    <h1 class="mb-5 text-center">Etiquettes Casier ✅</h1>
    <a class="btn btn-primary impression-no mb-5 ml-5" href="{{ path('etiquettes') }}">Retour</a>
    <div class="impression-no text-center text-secondary">Impression paysage</div>
    {% set itemsbyline = 0 %}
    {% set count = 1 %}
    {% for student in students %}

            {% if select == 'ALL' or select == student.level %}
        {% set itemsbyline = itemsbyline + 1 %}
        {% if itemsbyline == 1 %}

        <div class="ml-3 row">
            {% endif %}
        {{ include('etiquettes/_casier.html.twig') }}
                {% if count == 8 %}
                    {% set count = 1 %}
                {% else %}
                    {% set count = count + 1 %}
                {% endif %}
            {% endif %}
        {% if itemsbyline == 2 %}

    </div>
            {% set itemsbyline = 0 %}
        {% endif %}

        {% endfor %}
    {{ itemsbyline == 0 ? '' : '</div>' }}

</div>

{% endblock %}
