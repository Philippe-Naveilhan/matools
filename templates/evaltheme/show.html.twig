{% extends 'base.html.twig' %}

{% block title %}Evaltheme{% endblock %}

{% block body %}
    <h1>Evaltheme</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ evaltheme.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ evaltheme.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('evaltheme_index') }}">back to list</a>

    <a href="{{ path('evaltheme_edit', {'id': evaltheme.id}) }}">edit</a>

    {{ include('evaltheme/_delete_form.html.twig') }}
{% endblock %}
