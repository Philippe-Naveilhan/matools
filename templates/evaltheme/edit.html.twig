{% extends 'base.html.twig' %}

{% block title %}Modification de thème{% endblock %}

{% block body %}
    <h1 class="text-center my-5">Modification du thème :<br> "{{ evaltheme.name }}"</h1>
    <div class="text-center">
    {{ include('evaltheme/_form.html.twig', {'button_label': 'Modifier'}) }}

        {{ include('evaltheme/_delete_form.html.twig') }}

    </div>


    <a class="button button-dyna" href="{{ path('evaluation_showarbo', {'id': evaltheme.evaluation.id}) }}">Retour</a>
{% endblock %}
