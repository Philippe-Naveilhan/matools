{#{{ form_start(form) }}#}
{#    {{ form_widget(form) }}#}

{#{% if themes is defined %}#}

<form name="evaluation" method="post">
    <div id="evaluation">
        <div class="text-center">
            <input type="text" id="evaluation_name" name="evaluation[name]" required="required" maxlength="255" placeholder="Nom de l'évaluation"/>
        </div>
        <div><input type="hidden" id="classroom_id" name="classroom_id" value="{{ classroom.id }}"> </div>
    </div>

    <button class="btn">Enregistrer</button>
</form>
{#{% endif %}#}

{#    <button class="btn">{{ button_label|default('Save') }}</button>#}
{#{{ form_end(form) }}#}
